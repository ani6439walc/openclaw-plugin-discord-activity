{
  "id": "discord-tool-status",
  "name": "Discord Tool Status",
  "description": "Shows live tool-call status as a Discord message that is updated and deleted when the agent finishes.",
  "activation": {
    "onStartup": true,
    "onChannels": ["discord"]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "maxToolHistoryLength": {
        "type": "integer",
        "minimum": 1,
        "maximum": 50,
        "default": 30,
        "description": "Maximum number of tool calls to keep in history"
      },
      "maxStatusMessageLength": {
        "type": "integer",
        "minimum": 100,
        "maximum": 1700,
        "default": 1700,
        "description": "Maximum length of Discord status message content, capped at 1700 characters"
      },
      "orphanTtlMs": {
        "type": "integer",
        "minimum": 1,
        "default": 300000,
        "description": "Time in milliseconds before orphaned tool calls are pruned"
      },
      "maxDisplayMs": {
        "type": "integer",
        "minimum": 1000,
        "default": 600000,
        "description": "Maximum time in milliseconds a status message may remain before automatic cleanup"
      }
    }
  }
}
