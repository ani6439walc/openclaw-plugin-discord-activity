{
  "id": "discord-tool-status",
  "name": "Discord Tool Status",
  "description": "Shows live tool-call status as a Discord message that is updated and deleted when the agent finishes.",
  "activation": {
    "onStartup": false
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "maxToolHistoryLength": {
        "type": "integer",
        "minimum": 1,
        "maximum": 50,
        "default": 10,
        "description": "Maximum number of tool calls to keep in history"
      },
      "maxStatusMessageLength": {
        "type": "integer",
        "minimum": 100,
        "maximum": 4000,
        "default": 1700,
        "description": "Maximum length of the Discord status message content"
      },
      "orphanTtlMs": {
        "type": "integer",
        "minimum": 1,
        "default": 300000,
        "description": "Time in milliseconds before orphaned tool calls are pruned"
      },
      "activeMemoryEnabled": {
        "type": "boolean",
        "default": true,
        "description": "Whether to show active-memory indicator in status messages"
      }
    }
  }
}
